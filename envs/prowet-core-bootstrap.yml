# Bootstrap only. Not a complete nine-model installation or a validated fresh install.
# The validated route clones an existing screening environment; see docs/minimal_environment_zh.md.
name: prowet-core
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10.21
  - pip
  - pip:
      - -r envs/minimal-core-requirements.txt
      - -e .
